var express = require('express');
 
var app = express();
 
app.get('/', function (req, res) {
	res.send('Hello Jenkins Again');
    });

//added a coment to check if the jenkins working
app.listen(process.env.PORT || 5000);
 
module.exports = app;
